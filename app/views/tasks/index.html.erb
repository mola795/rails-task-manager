<h1>All Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %> - <%= task.completed ? 'Yes' : 'No' %>
      | <%= link_to 'Edit', edit_task_path(task) %>
    </li>
  <% end %>
</ul>

<%= link_to 'New Task', new_task_path %>
